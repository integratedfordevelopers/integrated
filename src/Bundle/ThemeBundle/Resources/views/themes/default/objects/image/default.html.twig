{% block image %}
    <img src="{{ web_image(document.file).cropResize(1140, 600) }}" alt="{{ document.title|default }}" title="{{ document.description|default }}" class="{{ class|default }}" />
{% endblock %}
