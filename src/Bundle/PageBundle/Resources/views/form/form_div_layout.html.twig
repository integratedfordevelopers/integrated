{% block _integrated_page_page_path_widget %}
    <div class="input-group">
        <div class="input-group-addon">/</div>
        <input type="text" {{ block('widget_attributes') }} class="form-control" {% if value is not empty %}value="{{ value }}" {% endif %}>
    </div>
{% endblock %}

{% block page_copy_pages_row %}
    {% apply spaceless %}
        <table class="table table-hover" id="copy-table">
            <colgroup>
                <col style="width: 2em" />
                <col style="width: 5em" />
                <col />
                <col />
                <col style="width: 40em" />
            </colgroup>

            <thead>
            <tr>
                <th></th>
                <th>{% trans %}Action{% endtrans %}</th>
                <th>{% trans %}Title{% endtrans %}</th>
                <th>{% trans %}URL{% endtrans %}</th>
                <th>{% trans %}Blocks{% endtrans %}</th>
            </tr>
            </thead>
            <tbody>

            {% for field in form %}

                {{ form_row(field) }}

            {% endfor %}

            </tbody>
        </table>
    {% endapply %}
{% endblock %}

{% block page_copy_page_row %}
    {% apply spaceless %}
        <tr>
            <td>
                {{ form_widget(form.selected) }}
            </td>
            <td>
                {{ copyAction }}
            </td>
            <td>
                {{ page.title }}
            </td>
            <td>
                {{ page.path }}
            </td>
            <td class="copy-pages-blocks">
                {% for field in form %}

                    {{ form_row(field) }}

                {% endfor %}
            </td>
        </tr>
    {% endapply %}
{% endblock %}

{% block page_copy_block_row %}
    {% apply spaceless %}
        <div class="row">
            <div class="col-md-4">
                {{ block.id|truncate(20) }}
            </div>
            <div class="col-md-4">
                {{ form_widget(form.operation) }}
            </div>
            <div class="col-md-4">
                {{ form_widget(form.newBlockId) }}
            </div>
        </div>
    {% endapply %}
{% endblock %}

