{% if app.request.attributes.get('integrated_block_edit') %}
    <div class="panel panel-danger">
        <div class="panel-heading">
            {% trans with { '%title%': block.title|default('undefined'), '%id%': id } %}Block "%title%" (id: %id%) contains an error{% endtrans %}
        </div>
    </div>
{% endif %}
