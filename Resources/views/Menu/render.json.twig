{% spaceless %}
{% set data = {
    'data'   : menu.toArray|default([]),
    'options': options,
    'html'   : knp_menu_render(menu, options)|trim,
} %}

{{ data|json_encode|raw }}
{% endspaceless %}
