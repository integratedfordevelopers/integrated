<script type="text/x-handlebars-template" id="integrated_website_template_modal_block_add">
    {% verbatim %}
    {{#each blocks}}
    <div class="row form-group">
        <div class="col-sm-8">
            <b>{{ title }}</b>
            <p>{{ type }}</p>
        </div>
        <div class="col-sm-4">
            <a href="javascript:;" data-action="integrated-website-block-choose" data-id="{{ id }}" class="btn btn-default pull-right">Choose</a>
        </div>
    </div>
    {{/each}}
    {% endverbatim %}

    <div class="row form-group">
        <div class="col-sm-12">
            <a href="{{ path('integrated_block_block_index') }}" class="btn btn-default pull-right">New</a>
        </div>
    </div>
</script>
