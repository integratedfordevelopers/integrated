{% extends 'IntegratedContentBundle::base.html.twig' %}
{% block body %}
    <ol class="breadcrumb">
        <li>
            <a href="{{ path('integrated_content_content_type_index') }}">{% trans %}Home{% endtrans %}</a>
        </li>
        <li>
            <a href="{{ path('integrated_content_content_type_show', {'id':  contentType.id}) }}">
                {{ contentType.name }}
            </a>
        </li>
        <li class="active">{% trans %}Edit relation{% endtrans %}</li>
    </ol>
    <div class="page-header">
        <h1>{% trans %}Edit relation{% endtrans %}</h1>
    </div>
    {{ form(form, { 'style': 'horizontal' }) }}
    </div>
{% endblock %}