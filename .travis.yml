install:
- curl -sS https://getcomposer.org/installer | php
before_script:
- php7.1 composer.phar update -n
script:
- vendor/bin/phpcs
- vendor/bin/php-cs-fixer fix --verbose --show-progress=none --dry-run
- vendor/bin/phpunit -c phpunit.xml.dist
