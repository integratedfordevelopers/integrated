install:
- curl -sS https://getcomposer.org/installer | php
before_script:
- php composer.phar update -n
script:
- vendor/bin/phpunit -c phpunit.xml
- vendor/bin/phpcs --standard=PSR2 --severity=error .
